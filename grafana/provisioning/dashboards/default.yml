apiVersion: 1
providers:
  - name: 'Auth Services'
    orgId: 1
    folder: 'Auth Services'
    folderUid: 'auth-services'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards